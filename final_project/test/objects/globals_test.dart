import 'dart:ui';

import 'package:final_project/objects/group.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:final_project/objects/globals.dart';

void main() {
  // test('createGroups adds group to events and assignmetns', () {
  //   Group test = const Group(name: "Test", color: Color(0xFF0F8644), age: 1);

  //   createGroup(test);

  //   expect(events.containsKey(test), true);
  //   expect(assignments.containsKey(test), true);
  // });
  // test('indexGroups looks up group by name', () {
  //   Group test = const Group(name: "Test", color: Color(0xFF0F8644), age: 1);

  //   createGroup(test);

  //   expect(indexGroups('Test'), test);
  //   expect(indexGroups('Not Test'), null);
  // });
}
